{
  "name": "@price-comparator/database",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "validate": "prisma validate",
    "format": "prisma format",
    "generate": "prisma generate", "build": "prisma generate"
  },
  "devDependencies": {
    "prisma": "^5.14.0"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "zod": "^4.4.3"
  }
}
